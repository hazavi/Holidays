@page "/AddBirthday"
@using MærkeDag.Data
@using System.Globalization;
@inject BirthdayDbService bdayService
@inject NavigationManager NavManager

<h3>Add Birthday</h3>
<hr />

<form>
    <div class="row">
        <div class="cold-md-8">

            <div class="for-group">
                <label for="Name" class="control-label">Navn: </label>
                <input form="Name" class="form-control" placeholder="Indtast Navn" @bind=@obj.Navn/>
            </div>
            <div class="for-group">
                <label for="birthday" class="control-label">Birthday</label>
                <input form="birthday" class="form-control" type="date" @bind=@obj.Følselsdag />
            </div>
        </div>
    </div>

    <div class="row">
        <div class="col-md-4">
            <div class="form-group">
                <input type="button" class="btn btn-primary" @onclick="@CreateBirthday" value="Save" />
                <input type="button" class="btn btn-danger" @onclick="@Cancel" value="Cancel" />
            </div>
        </div>
    </div>

</form>

@code {
    Birthday obj = new Birthday();

    protected override void OnInitialized()
    {
        obj.Følselsdag = DateTime.Now;
    }

    protected async void CreateBirthday()
    {
        await bdayService.AddBirthday(obj);
        NavManager.NavigateTo("Birthday");
    }
    protected async void Cancel()
    {
       NavManager.NavigateTo("Birthday");

    }
}
